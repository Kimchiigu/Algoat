export const handlePost = async (
  senderId: String,
  title: String,
  contents: Text,
  file: String,
  fileName: String,
  fileType: String
) => {
  try {
  } catch (error) {
    console.log(error);
  }
};
